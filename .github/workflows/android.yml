name: Build

on:
  push:
    branches: ['**']
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      # Zero dependencies: these import the app's own ESM modules directly.
      - name: Unit tests
        run: node --test tests/*.test.mjs

      # Two separate things: the package has to be resolvable by the test's
      # require(), and the browser binary has to be downloaded. `playwright
      # install` alone only does the second.
      - name: Install Playwright and Chromium
        run: |
          npm install --no-save playwright
          npx playwright install --with-deps chromium

      # Drives the real UI: IndexedDB persistence across a reload, the session
      # logger, PR detection, charts, and the backup round trip.
      - name: Browser smoke test
        run: node tests/browser-smoke.mjs

  android:
    name: Android APK
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - uses: gradle/actions/setup-gradle@v4

      - name: Make the wrapper executable
        run: chmod +x ./gradlew

      - name: Build debug APK
        run: ./gradlew --no-daemon assembleDebug

      # Uploaded before lint runs: lint is slow, and a lint failure should not
      # cost you an APK that already built.
      - uses: actions/upload-artifact@v4
        with:
          name: workoutmate-debug-apk
          path: app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error

      - name: Lint
        run: ./gradlew --no-daemon lintDebug

      - name: Upload lint report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: lint-report
          path: app/build/reports/lint-results-debug.html
          if-no-files-found: ignore
